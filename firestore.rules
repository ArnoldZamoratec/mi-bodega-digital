rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Esta regla permite leer y escribir SIEMPRE
    // si el usuario está conectado (request.auth no es nulo)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}